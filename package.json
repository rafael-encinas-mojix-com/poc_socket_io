{
  "name": "socketio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "mkdir -p dist && cp -R node_modules src server.js ... dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "kafkajs": "^1.15.0",
    "socket.io": "^4.3.0"
  }
}
